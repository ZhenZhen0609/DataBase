# DBMS项目 - 阶段五任务分配

## 🎯 阶段五核心目标
完成DBMS最终版本，实现完整的数据库管理系统功能，包括：
1. SQL DML语言支持
2. 查询优化与索引
3. 数据完整性约束
4. 增强的用户界面
5. 系统性能优化

---

## 🔴 红圈A同学 - 存储管理与性能优化

### 🎯 核心职责
底层存储优化、索引实现、事务支持、性能提升

### 📋 具体任务清单

#### 第一周（5月6日-5月12日）
**高优先级任务**：
1. **索引系统实现** ⭐⭐⭐
   - 创建 `indexmanager.cpp/h` 模块
   - 实现哈希索引（主键自动索引）
   - 支持索引创建/删除：`createIndex()`, `dropIndex()`
   - 索引文件格式：`.tid` 扩展名

2. **查询性能优化** ⭐⭐⭐
   - WHERE条件推送到存储层
   - 实现LIMIT/OFFSET分页支持
   - 添加简单查询缓存机制
   - 更新 `storagemanager.cpp` 优化数据读取

3. **基础事务支持** ⭐⭐
   - 实现WAL（Write-Ahead Logging）机制
   - 支持BEGIN/COMMIT/ROLLBACK基本语法
   - 在 `ruanko.log` 中记录事务操作

#### 第二周（5月13日-5月19日）
**中优先级任务**：
4. **并发控制** ⭐⭐
   - 添加读写锁机制（`QReadWriteLock`）
   - 支持多用户并发访问
   - 防止数据竞争的基本保护

5. **系统监控** ⭐
   - 添加性能统计功能
   - 实现简单的系统状态监控
   - 记录查询执行时间统计

**低优先级任务**：
6. **数据备份机制** ⭐
   - 实现数据库备份功能
   - 支持备份文件恢复

### 📁 交付文件
- `indexmanager.cpp/h` - 索引管理模块
- 更新 `storagemanager.cpp/h` - 添加索引支持
- 更新 `common.h` - 添加索引数据结构
---

## 🟠 橙圈B同学 - SQL解析与查询引擎

### 🎯 核心职责
SQL DML解析、查询执行、条件处理、错误管理

### 📋 具体任务清单

#### 第一周（5月6日-5月12日）
**高优先级任务**：
1. **SQL DML解析** ⭐⭐⭐
   - SELECT语句：`SELECT * FROM table WHERE ...`
   - INSERT语句：`INSERT INTO table VALUES (...)`
   - UPDATE语句：`UPDATE table SET ... WHERE ...`
   - DELETE语句：`DELETE FROM table WHERE ...`

2. **WHERE条件解析** ⭐⭐⭐
   - 比较运算符：`=, <>, >, <, >=, <=`
   - 逻辑运算符：`AND, OR, NOT`
   - 支持 `IN`, `BETWEEN`, `LIKE`
   - 创建 `conditionparser.cpp/h` 模块

3. **查询执行引擎** ⭐⭐
   - 实现查询计划生成
   - 结果集排序：`ORDER BY`
   - 聚合函数：`COUNT(), SUM(), AVG(), MAX(), MIN()`

#### 第二周（5月13日-5月19日）
**中优先级任务**：
4. **高级查询功能** ⭐⭐
   - GROUP BY分组支持
   - HAVING子句过滤
   - DISTINCT去重
   - 子查询基础支持

5. **SQL错误处理** ⭐⭐
   - 完善语法错误提示
   - 语义检查（表/字段存在性）
   - 友好的错误消息系统

**低优先级任务**：
6. **SQL扩展功能** ⭐
   - JOIN基础支持（INNER JOIN）
   - UNION操作
   - 视图基础支持

### 📁 交付文件
- 更新 `sqlparser.cpp/h` - 支持DML语法
- `queryengine.cpp/h` - 查询执行引擎
- `conditionparser.cpp/h` - 条件解析器
- `SQL测试用例.txt` - 完整测试集
---

## 🔵 蓝圈C同学 - 记录管理与数据操作

### 🎯 核心职责
数据操作优化、完整性约束、数据验证、导入导出

### 📋 具体任务清单

#### 第一周（5月6日-5月12日）
**高优先级任务**：
1. **高级数据操作** ⭐⭐⭐
   - 批量插入：支持多行INSERT
   - 条件更新：UPDATE带WHERE子句
   - 条件删除：DELETE带WHERE子句
   - 事务支持的数据操作

2. **数据完整性约束** ⭐⭐⭐
   - 外键约束支持
   - 唯一性约束：`UNIQUE`
   - 检查约束：`CHECK`
   - 级联删除/更新

3. **数据验证增强** ⭐⭐
   - 严格的类型验证
   - 默认值约束：`DEFAULT`
   - 非空约束：`NOT NULL`
   - 数据格式验证（邮箱、日期等）

#### 第二周（5月13日-5月19日）
**中优先级任务**：
4. **数据导入导出** ⭐⭐
   - CSV文件导入功能
   - JSON数据导出功能
   - 数据备份/恢复功能

5. **数据迁移工具** ⭐
   - 表结构迁移
   - 数据批量迁移
   - 兼容性检查

**低优先级任务**：
6. **数据加密** ⭐
   - 敏感数据加密存储
   - 加密密钥管理

### 📁 交付文件
- 更新 `recordmanager.cpp/h` - 高级数据操作
- `constraintmanager.cpp/h` - 约束管理
- `dataimporter.cpp/h` - 数据导入导出
- `完整性测试用例.txt` - 约束测试
---

## 🟣 粉圈D同学 - UI完善与用户体验

### 🎯 核心职责
用户界面优化、数据可视化、用户体验、帮助系统

### 📋 具体任务清单

#### 第一周（5月6日-5月12日）
**高优先级任务**：
1. **SQL编辑器** ⭐⭐⭐
   - 语法高亮显示
   - 关键字自动补全
   - 查询历史记录
   - 执行结果分页显示

2. **数据可视化** ⭐⭐⭐
   - 图表显示：柱状图、折线图
   - 数据统计面板
   - 数据透视表基础
   - 数据筛选器

3. **用户体验优化** ⭐⭐
   - 进度条显示（长时间操作）
   - 撤销/重做功能
   - 多主题切换（深色/浅色）
   - 快捷键支持

#### 第二周（5月13日-5月19日）
**中优先级任务**：
4. **帮助与文档** ⭐⭐
   - 内置帮助系统
   - SQL语法提示
   - 用户手册
   - 示例数据库

5. **管理功能界面** ⭐
   - 用户管理界面
   - 数据库管理面板
   - 系统设置界面

**低优先级任务**：
6. **高级可视化** ⭐
   - 关系图显示
   - 数据分布图
   - 实时监控面板

### 📁 交付文件
- 更新 `mainwindow.cpp/h` 和 `.ui` 文件
- `sqleditor.cpp/h` - SQL编辑器组件
- `chartwidget.cpp/h` - 图表组件
---

## 🚨 风险应对策略

### 技术风险：
1. **索引实现复杂** → 先实现简单哈希索引，B+树作为备选
2. **SQL解析困难** → 使用现有解析库或简化语法
3. **性能问题** → 优先保证功能，性能优化逐步进行
4. **UI响应慢** → 使用异步加载和进度提示
---

## 📝 最终交付物清单

### 代码部分：
1. 完整的源代码（GitHub仓库）
2. 编译可执行文件（Release版本）
3. 所有测试用例和测试数据

### 文档部分：
1. 用户手册（PDF+在线版）
2. 开发文档（接口说明、架构设计）
3. 测试报告（功能测试+性能测试）
4. 部署指南（环境要求、安装步骤）

### 演示材料：
1. 项目演示PPT
2. 功能演示视频（5-10分钟）
3. 现场演示脚本

---

最新更新：完成了阶段五第一周的内容，新加了两个带内存缓存的高速读写接口 readTableData 和 writeTableData。之后在做数据查询和更新的时候直接调这两个，不用再去读写 .trd 物理文件了